<form action="/" method="post">
    <div>
        <p>Доступные мероприятия</p>
        <select class="form-control">
            <option value="value">text</option>@*здесь цикл*@
        </select>
        <p>Мои собаки</p>
        <select class="form-control">
            <option value="value">text</option>@*здесь цикл*@
        </select>
    </div>
    <div>

    </div>
    <input class="btn btn-primary form-control" type="submit" name="name" value="отправить заявку" />
</form>

<p>Создание документов</p>

<div>
    Выберете мероприятие:
    <div>
        <select class="form-control" ng-repeat="event in events" ng-model="forDocument.eventName">
            <option value="{{event.EventId}}">{{event.EventName}}</option>@*здесь цикл*@
        </select>
    </div>
    <div ng-if="IsEvent()">
        Выберете собаку:
        <select class="form-control" ng-model="forDocument.dogName">
            <option ng-repeat="dog in dogs" value="{{dog.DogId}}">{{dog.DogName}}</option>@*здесь цикл*@
        </select>
        Введите фамилию, имя, отчество сопровождающего:
        <input class="form-control" type="text" name="leaderFIO" placeholder="Фамилия Имя Отчество" ng-model="forDocument.leaderFIO" />

    </div>
    <button class="btn btn-primary form-control" id="createDocument" ng-click="createDocument()">Создать документ</button>

</div>